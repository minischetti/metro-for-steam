"resource/layout/broadcast_chatbanner.layout"
{
    colors
    {
        ChatTextLineBG = "0 0 0 80"
    }

    styles
    {
        ChatTextLine
        {
            render
            {
                // nothing
            }

            render_bg
            {
                0="fill( x0, y0, x1, y1, ChatTextLineBG )"
            }
        }
    }

    // layout is done in code
}